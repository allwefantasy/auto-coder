include_file: 
   - ./common/local.yml

index_filter_level: 1
query: |   
   Anything2Images 的具体用法为：
   
   v = Anything2Images(llm=llm,args=args)
   html = v.to_html("/Users/allwefantasy/Downloads/训练测试WORD-脱敏版/测试报告：L银行小额消费分析.docx")

   在 auto_coder.py,command_args.py 中添加一个doc2html的命令，该命令可以将word/pdf文档转换为html文档，并保存到指定的文件夹中。
   具体使用方法：
   
   ```bash
   auto-coder doc2html --url xxx.docx --output /tmp/
   ```   
   