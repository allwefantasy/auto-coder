
include_file:
  - ./base/base.yml

auto_merge: editblock 
human_as_model: true
skip_build_index: true
skip_confirm: true

urls:
- /Users/allwefantasy/projects/auto-coder/src/autocoder/chat_auto_coder.py

query: |
  添加一个 /conf 命令,使用方式为  /conf auto_merge: editblock  然后保存到 memory 字段里去
