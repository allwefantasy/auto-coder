
include_file:
  - ./base/base.yml

auto_merge: editblock 
human_as_model: true
skip_build_index: true
skip_confirm: true

urls:
- /Users/allwefantasy/projects/auto-coder/src/autocoder/chat_auto_coder.py

query: |
  index_query里面不使用临时文件,而是在actions目录使用uuid作为文件名构建一个文件,使用完后删除
