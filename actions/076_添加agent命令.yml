include_file: 
   - ./common/local.yml

human_as_model: true
auto_merge: editblock

# model: qwen2_chat

query: |   
   修改 auto_coder.py,byzerllm_client.py,command_args.py,common/__init__.py  等几个文件，
   新增一个 agent 命令，实现暂时为空。
   新增一个 query_prefix 和 query_suffix 参数，用于指定查询的前缀和后缀。