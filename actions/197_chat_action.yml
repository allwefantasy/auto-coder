auto_merge: editblock
human_as_model: true
include_file:
- ./base/base.yml
query: new_words = text[len("/conf /remove") :].strip().split() 这段代码有问题,需要支持 /drop
  /remove /drop
skip_build_index: true
skip_confirm: true
urls:
- /Users/allwefantasy/projects/auto-coder/src/autocoder/chat_auto_coder.py
