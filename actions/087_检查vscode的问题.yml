auto_merge: editblock
human_as_model: true
project_type: .ts,.json,.js 
include_file:
- ./common/local.yml

urls:
- /Users/allwefantasy/projects/auto-coder/ide_plugins/vscode/auto-coder/.vscodeignore
- /Users/allwefantasy/projects/auto-coder/ide_plugins/vscode/auto-coder/tsconfig.json

exclude_files:      
  - regex://.*/conversations.json

query: |
  在 vscode 目录下的 auto-coder vscode插件，

  我们使用了 terminal.sendText(`cd ${projectRoot}`); 来设置项目目录。

  但是这种方式无法跨平台，所以我们需要一个更好的方式来设置项目目录。
  
  注意：同时你还要关注 package.json,webpack.config.js
