auto_merge: editblock
human_as_model: true
project_type: .ts,.json,.js 
include_file:
- ./common/local.yml

exclude_files:      
  - regex://.*/conversations.json

query: |
  在 vscode 目录下的 auto-coder vscode插件，

  在创建需求时，需要检测当前工作区是不是存在一个 .auto-coder 文件夹。
  如果不存在，则提示用户需要先初始化项目。
  
  注意：同时你还要关注 package.json,webpack.config.js
