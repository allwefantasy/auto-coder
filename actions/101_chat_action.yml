
include_file:
  - ./base/base.yml

auto_merge: editblock 
human_as_model: true
skip_build_index: true
skip_confirm: true

urls:
- /Users/allwefantasy/projects/auto-coder/src/autocoder/chat_auto_coder.py

query: |
  把yaml中的 auto_merge 到 skip_confirm 这四个参数抽取出来,可以使用命令 /conf auto_merge: editblock 这样来设置
